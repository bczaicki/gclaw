[package]
name = "gclaw-channels"
version = "0.1.0"
edition = "2021"
license = "LGPL-2.1"

[dependencies]
gclaw-core = { path = "../gclaw-core" }
tokio = { version = "1", features = ["sync"] }
async-trait = "0.1"
tracing = "0.1"
